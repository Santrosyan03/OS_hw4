#include <stdio.h>
#include "math_utils.h"

int main() {
    int number = 7;
    int number_squared = squareMethod(number);
    printf("Digit: %d, Square: %d", number, number_squared);
    return 0;
}
